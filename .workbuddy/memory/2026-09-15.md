# 2026-09-15

## fantasy-blueprint（奇幻造物实现蓝图）仓库

- 项目性质：纯静态 SPA，hash 路由，无后端。入口 `index.html`，数据层 `js/data/*.js`，视图层 `js/views/*.js`，`js/router.js` + `js/app.js`。
- 部署：GitHub Pages，从 `main` 分支根目录发布，地址 `https://blackinkvc.github.io/fantasy-blueprint/`。
- **关键坑（已踩并修复）**：数据文件（`js/data/techs.js`/`works.js`/`tree-template.js`）里的中文文本若内嵌 ASCII 双引号 `"`，会提前截断字符串 → 整个文件解析失败 → `TECHS`/`WORKS`/`TREE_BRANCHES` 等全局 undefined → 所有视图渲染空白。
  - 修复方式：用字符串状态机把"两侧被内容包围的内嵌引号"替换为全角 `“”`（真实闭引号判定 = 其后紧接 `, : } ] )` 或行尾）。
  - 验证：`node --check` 全过 + 用 DOM 桩跑全部 10 条路由确认 `app.innerHTML` 非空。
  - **以后编辑数据文件务必用全角引号 `“”` 或转义，别用 ASCII `"`。**

## GitHub 部署凭据
- 本机仍无 GitHub 凭据（keychain 空；SSH 私钥未在 `blackinkvc` 注册，Permission denied）。但用户此前已自行成功推送引号修复版，故部署以用户本地 `git push origin main` 为准。
- 本次"删除实现难度分级"改动已本地提交（664cd05），待用户推送后线上生效；推送后浏览器需强刷（Cmd+Shift+R）清旧缓存。

## 删除"实现难度分级"体系（用户明确指令）
- 用户认为 L1–L5 实现难度分级无意义，要求全站删除。
- 已删除：`js/data/levels.js`、`js/views/map.js`(进度星图)、`js/views/progress-charts.js`(分级图表)；`index.html` 移除对应 script/导航；`app.js` 移除 `/map` 路由。
- 数据层：移除 `techs.js` 全部 `level:"Lx"`(79 条) 与 `works.js` 的 `techLevels` 字段；`tree-template.js` 节点 `level:1-5` 字段一并移除。
- 视图层：home/category/detail/tech-tree/works/work/sop/audit/work-tree 移除分级卡片、过滤、徽章、色条、图例、统计表与实现进度图；work-tree 改为按领域挂载、移除实现等级罗马数字。
- 验证：`node --check` 全过 + DOM 桩跑 12 条路由全过（ROUTES OK=12 BAD=0）。
- 注意：进度星图页与分级图表是整页删除（不仅隐藏），因其本质就是分级可视化。

## 删除"六阶段研发流程/工程之道/判定之道/方法论"体系（用户明确指令）
- 用户认为六阶段 SOP、工程之道、判定之道均无意义，要求全站删除。
- 因研发SOP页（sop.js）本质就是这三项内容，采用整页删除：删除 `js/views/sop.js`、`js/data/methods.js`（METHOD_GROUPS/METHODS/STAGE_METHODS 全删）；`index.html` 移除「研发SOP」导航与 `methods.js`/`sop.js` 两个 script；`app.js` 移除 `/sop` 路由。
- `detail.js` 移除 `methodChips` 计算（依赖已删的 `getMethod`/`STAGE_METHODS`）与「对应研发阶段」区块；`techs.js` 移除全部 79 处 `sopStage` 字段；`css` 移除 SOP 页与方法论核心样式块；README 与 index.html meta 去除方法论措辞。
- 验证：`node --check` 全过 + DOM 桩渲染剩余 8 条路由全过（ROUTES OK=8 BAD=0）。
- 提交 `b0a82f0`（本地，待推送）。

## 工具坑：同文件并行 Edit 会丢失
- 一条消息里对同一个文件发多个 Edit 时，部分编辑会静默不生效（工具报 success 但实际未改），与先前 work.js 现象一致。
- 对策：同一文件的多个改动必须逐个串行发送（一次一个 Edit），或跨文件配对并行。已在本轮用此法补救 README/detail.js 漏改。

## 增加 100 个经典奇幻世界（用户明确指令）
- 用户要求"增加 100 个经典奇幻世界"。目标文件 `js/data/works.js`（原 16 条 → 现 116 条）。
- 做法：用生成脚本 `/tmp/gen_works.js` 定义 100 条 `W` 数组（神话16/经典文学20/现代小说34/游戏14/动漫12/漫画4 分类），每条含完整 WORKS schema 字段；脚本内置校验（id 去重、treeLit 必须匹配 `^[FBLRWAR][1-5]$`、总数=100），用 `lastIndexOf("];")` 定位数组结尾追加。
- 踩坑修正：① 初版 101 条（游戏段多 1），删 `guild-wars` 回到 100；② 脚本 `fmt()` 原读 `w.physicalDivergences`/`w.representativeTechs`，但数据用简写 `div`/`rep` → 改为读简写，输出仍用标准字段名。
- 数据说明：新增条目的 `representativeTechs` 多为自由 id（如 olympian/yggdrasil），大多不在 `TECHS` 中 → work.js/work-tree.js 安全降级为 `<span class="tag">`，不崩；`treeLit` 全部为合法 30 节点 id，会在造物树点亮对应分支。
- 验证：`node --check js/data/works.js` 通过；字段完整性（11 字段齐全、0 重复 id、0 非法 treeLit）；DOM 桩渲染：WorksView 全部筛选 + 116 条 WorkView 详情 + 造物树(抽20) + TechTreeView + Home/Category/Detail/Relations/Audit 全 OK（BAD=0）。

## 「技术」→「概念」术语统一 + 每世界再扩充 10 条概念（用户明确指令）
- 用户：① 全站「技术」改为「概念」；② 给每个奇幻世界再扩充 10 条概念，要准确、不瞎编（全量完整卷宗：每条含 name/领域/准确说明/原理+现实可行性 verdict）。
- 「技术」→「概念」：UI 文案与注释在更早一轮已改（README/detail/tech-tree/work/work-tree/audit）；本轮补漏——把 `techs.js` 内 `achieved` verdict 的 `implementation.current` 文案「现实已有对应技术或原理成立」等共 433 处中文「技术」也统一为「概念」（该文案在详情页实现路径区用户可见）。改完 repo 内「技术」0 残留（仅保留英文标识符 `TECHS` 不动）。
- 概念扩充：生成器 `/tmp/gen_concepts.js` 在 `CONCEPTS` 数组里为 116 个世界各写 10 条（紧凑 `{w,n,d,s,f}` 格式），`implFor()` 按 verdict 生成 `implementation`；末尾校验 id 去重 / domain∈6类 / verdict∈3类。本轮补齐最后 30 个世界（游戏14+动漫12+漫画4=300 条），脚本注入 `js/data/techs.js`。
- **TECHS 由 79 → 1239**（新增 1160 条：此轮 300 + 此前 860）。
- 数据修复（务必记牢，前几轮脚本遗留）：
  1. 8 条 `oz` 世界旧条目（`oz-witch6` 等）曾丢失 `id` 且 `workId` 错写成概念 id（如 `workId:"oz-witch6"`）→ 全部修正 `workId:"oz"`，并补回 `id`（已在 techs.js + 生成器双修）。
  2. `ancient-magus` 新批次用了 `am-` 前缀，与既有的 `aztec-myth`、`amber` 两条目 `am-` 前缀撞车（重复 `am-sacrifice`）→ 新批次改前缀 `magus-`。
  3. 校验脚本 `validate_data.js` 已确认：116 世界全部 ≥10 条、0 非法 workId/domain/verdict、0 重复 id。
- 验证：`node --check` 全过；`validate_data.js` 0 错；DOM 桩跑全部 9 个视图 render 全 OK（含 Audit 93 万字符、Category 52 万字符，数据量下不崩）。
- 提交 `5004c2e`（本地，待用户 `git push origin main`）。
- **遗留待办**：`starless-sea`（星之海）那 10 条 `ss2-*` 是更早一轮的退化占位（说明如「以结、重的局」语义残缺），不准确，建议后续按真实设定重写或删除——已不在本轮范围。
- 提交本地（本次），推送由用户 `git push origin main` 执行；线上需强刷。

## 德累斯顿档案（dresden）重点扩充（用户：特别喜欢这故事，务必准确）
- 原 10 条 `dr-*` 概念偏弱/泛化（战杖施法、灵魂火等），用户要求着重补充且务必准确（不瞎编）。
- 本轮新增 19 条考据准确的 `dr2-*` 概念（TECHS 1239 → 1258）：圣白议会、魔法七律、议会卫兵、黑杖、十字圣剑(三圣钉/Amoracchius=Excalibur·Fidelacchius=Kusanagi→光剑·Esperacchius=Durendal)、十字骑士(Michael/Shiro/Sanya/Butters)、吸血鬼四庭(红/白/黑/玉)、白庭家族(Raith食lust·Malvora食fear·Skavis食despair，真爱克Raith)、仙灵双庭(夏Titania/冬Mab，Maeve→Molly Carpenter 接冬女，Lloyd Slate→哈利 接冬骑士)、莉南西迪(Lea教母)、永不对(Nevernever)、外者(Outer Gates/Walkers/Nemesis，星裔克之)、黑币团(三十银币/Nicodemus+Anduriel)、冬之骑士披风、魂火(Soulfire/Uriel)、恶魔礁(Demonreach监狱岛)、鲍勃(颅中智灵)、阿尔法狼群、星裔(Starborn)。
- 考据来源：本轮 4 次 WebSearch 核实七律/圣剑/吸血鬼四庭/仙灵双庭 + 补充 3 次核实圣剑别名与持剑者、白庭家族、冬女/冬骑士传承，关键事实（Excalibur/Kusanagi/Durendal 对应、Maeve→Molly、Slate→哈利、Thomas 为哈利同母异父弟、Mab 镇守外界之门）均经 wiki 交叉确认。
- 判定：超自然之力类一律 `violated`；组织/制度类（议会、卫兵、骑士团、黑杖、白庭情绪反制隐喻、冬庭平衡隐喻、契约/同盟隐喻）标 `achieved`。
- 验证：`node --check` 过；`validate_data.js` 0 错（116 世界全 ≥10）；DOM 桩 9 视图 render 全 OK。
- 提交 `af78f80`（本地，待推送）。

## 遗留（未了，来自更早的 SOP 删除）：index.html 死链
- `index.html` 仍残留死「研发SOP」导航(`<a href="#/sop">`) 与死 script(`<script src="js/views/sop.js">`)（sop.js 已删 → 404+控制台报错）。
- 此前已向用户说明：按领域拆层统计（造物领域 chips / 开发进度按领域表）**未**随 SOP 删除而丢失（home.js、audit.js 均仍在）。死链才是真遗留。
- 待用户拍板：① 删死链+死 script（推荐）；② 或指定新呈现位置。用户尚未确认，未动手改。

## 为全部 116 世界观补充 故事/梗概/撰写背景/作者情况（用户明确指令）
- 用户：每个世界观补充 故事(story)、梗概(synopsis)、撰写背景(background)、作者情况(author)。
- 做法：在 `js/data/works.js` 末尾（WORKS 数组 `];` 之后）追加 `const WORKS_META = { workId: {story,synopsis,background,author} }`，覆盖全部 116 个 id（与 WORKS 一一对应，校验 0 缺失 / 0 多余 / 0 空字段 / 0 内嵌 ASCII 双引号）。
- 内容：每条 4 字段各约 1 句，作者/年代/核心情节/创作背景均按已知事实撰写（神话与经典作家、游戏/动漫漫画原作者与厂商均核对）；以本馆 repo 既有「数据内嵌 ASCII 双引号会截断字符串」坑为戒，全用《》「」· 等中文标点，无 ASCII `"`。
- 视图：`js/views/work.js` 的 WorkView 新增「故事 · 梗概 · 撰写背景 · 作者」区块（`.meta-grid` 2×2 网格，窄屏单列），读取 `WORKS_META[w.id]`；`css/styles.css` 加 `.meta-grid/.meta-cell` 样式。
- 验证：`node --check` 全过；WORKS_META 与 WORKS id 交叉校验 0 错；DOM 桩渲染 WorkView(dresden/lotr/norse-myth/bone/got) 均含四标题且关键内容正确；9 视图 smoke 全 OK。
- 生成为临时脚本 `/tmp/works_meta.js`（后再合并入 works.js）。
- 提交 `b5b77bd`（本地，待用户 `git push origin main`；线上强刷）。
