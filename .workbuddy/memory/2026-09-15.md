# 2026-09-15

## fantasy-blueprint（奇幻造物实现蓝图）仓库

- 项目性质：纯静态 SPA，hash 路由，无后端。入口 `index.html`，数据层 `js/data/*.js`，视图层 `js/views/*.js`，`js/router.js` + `js/app.js`。
- 部署：GitHub Pages，从 `main` 分支根目录发布，地址 `https://blackinkvc.github.io/fantasy-blueprint/`。
- **关键坑（已踩并修复）**：数据文件（`js/data/techs.js`/`works.js`/`tree-template.js`）里的中文文本若内嵌 ASCII 双引号 `"`，会提前截断字符串 → 整个文件解析失败 → `TECHS`/`WORKS`/`TREE_BRANCHES` 等全局 undefined → 所有视图渲染空白。
  - 修复方式：用字符串状态机把"两侧被内容包围的内嵌引号"替换为全角 `“”`（真实闭引号判定 = 其后紧接 `, : } ] )` 或行尾）。
  - 验证：`node --check` 全过 + 用 DOM 桩跑全部 10 条路由确认 `app.innerHTML` 非空。
  - **以后编辑数据文件务必用全角引号 `“”` 或转义，别用 ASCII `"`。**

## GitHub 部署凭据
- 本机仍无 GitHub 凭据（keychain 空；SSH 私钥未在 `blackinkvc` 注册，Permission denied）。但用户此前已自行成功推送引号修复版，故部署以用户本地 `git push origin main` 为准。
- 本次"删除实现难度分级"改动已本地提交（664cd05），待用户推送后线上生效；推送后浏览器需强刷（Cmd+Shift+R）清旧缓存。

## 删除"实现难度分级"体系（用户明确指令）
- 用户认为 L1–L5 实现难度分级无意义，要求全站删除。
- 已删除：`js/data/levels.js`、`js/views/map.js`(进度星图)、`js/views/progress-charts.js`(分级图表)；`index.html` 移除对应 script/导航；`app.js` 移除 `/map` 路由。
- 数据层：移除 `techs.js` 全部 `level:"Lx"`(79 条) 与 `works.js` 的 `techLevels` 字段；`tree-template.js` 节点 `level:1-5` 字段一并移除。
- 视图层：home/category/detail/tech-tree/works/work/sop/audit/work-tree 移除分级卡片、过滤、徽章、色条、图例、统计表与实现进度图；work-tree 改为按领域挂载、移除实现等级罗马数字。
- 验证：`node --check` 全过 + DOM 桩跑 12 条路由全过（ROUTES OK=12 BAD=0）。
- 注意：进度星图页与分级图表是整页删除（不仅隐藏），因其本质就是分级可视化。

## 删除"六阶段研发流程/工程之道/判定之道/方法论"体系（用户明确指令）
- 用户认为六阶段 SOP、工程之道、判定之道均无意义，要求全站删除。
- 因研发SOP页（sop.js）本质就是这三项内容，采用整页删除：删除 `js/views/sop.js`、`js/data/methods.js`（METHOD_GROUPS/METHODS/STAGE_METHODS 全删）；`index.html` 移除「研发SOP」导航与 `methods.js`/`sop.js` 两个 script；`app.js` 移除 `/sop` 路由。
- `detail.js` 移除 `methodChips` 计算（依赖已删的 `getMethod`/`STAGE_METHODS`）与「对应研发阶段」区块；`techs.js` 移除全部 79 处 `sopStage` 字段；`css` 移除 SOP 页与方法论核心样式块；README 与 index.html meta 去除方法论措辞。
- 验证：`node --check` 全过 + DOM 桩渲染剩余 8 条路由全过（ROUTES OK=8 BAD=0）。
- 提交 `b0a82f0`（本地，待推送）。

## 工具坑：同文件并行 Edit 会丢失
- 一条消息里对同一个文件发多个 Edit 时，部分编辑会静默不生效（工具报 success 但实际未改），与先前 work.js 现象一致。
- 对策：同一文件的多个改动必须逐个串行发送（一次一个 Edit），或跨文件配对并行。已在本轮用此法补救 README/detail.js 漏改。

## 增加 100 个经典奇幻世界（用户明确指令）
- 用户要求"增加 100 个经典奇幻世界"。目标文件 `js/data/works.js`（原 16 条 → 现 116 条）。
- 做法：用生成脚本 `/tmp/gen_works.js` 定义 100 条 `W` 数组（神话16/经典文学20/现代小说34/游戏14/动漫12/漫画4 分类），每条含完整 WORKS schema 字段；脚本内置校验（id 去重、treeLit 必须匹配 `^[FBLRWAR][1-5]$`、总数=100），用 `lastIndexOf("];")` 定位数组结尾追加。
- 踩坑修正：① 初版 101 条（游戏段多 1），删 `guild-wars` 回到 100；② 脚本 `fmt()` 原读 `w.physicalDivergences`/`w.representativeTechs`，但数据用简写 `div`/`rep` → 改为读简写，输出仍用标准字段名。
- 数据说明：新增条目的 `representativeTechs` 多为自由 id（如 olympian/yggdrasil），大多不在 `TECHS` 中 → work.js/work-tree.js 安全降级为 `<span class="tag">`，不崩；`treeLit` 全部为合法 30 节点 id，会在造物树点亮对应分支。
- 验证：`node --check js/data/works.js` 通过；字段完整性（11 字段齐全、0 重复 id、0 非法 treeLit）；DOM 桩渲染：WorksView 全部筛选 + 116 条 WorkView 详情 + 造物树(抽20) + TechTreeView + Home/Category/Detail/Relations/Audit 全 OK（BAD=0）。
- 提交本地（本次），推送由用户 `git push origin main` 执行；线上需强刷。
